# Fix agent feedback review commands in editor windows

Upstream: [microsoft/vscode#336430](https://github.com/microsoft/vscode/pull/336430), fixing [issue #336428](https://github.com/microsoft/vscode/issues/336428).

State on 2026-09-16: **OPEN**. Snapshot head: `cc59706b4b6349329f7af86d159e654eb1864093`. Source base: `0a4fc0adc2c7ca7dc85fe5a271cf78f2ec36bc25`.

[Download source patch](../../source-patches/vscode/336430.patch) · [Current upstream patch](https://github.com/microsoft/vscode/pull/336430.patch)

Regular editor windows display the agent-host review confirmation but do not register its comment commands. The renderer logs a command-not-found failure, then incorrectly shows “No unreviewed comments” with Reveal Selected disabled. This was observed in Windows VS Code 1.137.0 connected to WSL; the registration gap also exists in the recorded upstream source base.

The fix registers editor-only handlers backed by the owning host's annotations channel. Host services load lazily. Reads wait for hydration and preserve remote file mapping and peer-chat ownership. Explicit selection is written before approval, empty or stale selections are rejected before mutation, and unchecked pending selections from interrupted reveals are cleared. Loading errors receive a distinct message. The dedicated Agents-window implementation remains separate.

## Validation and review

- 16 focused Chromium browser tests passed: lazy command registration, review filtering, WSL resource mapping, peer-session routing, delayed hydration, empty results, initial/asynchronous errors, disposal, explicit and stale selection, interrupted reveals, navigation/deletion, and renderer error/empty/disposal states.
- Targeted TypeScript diagnostics for all five changed files: zero.
- ESLint for the five changed files and Git whitespace checks passed.
- The published snapshot matches the fixed worktree (reverse-apply check).
- Pre-submission review checked startup side effects, layer boundaries, subscription lifetime, selection ordering, stale IDs and unchecked-comment disclosure. The review led to lazy host creation and clearing unchecked pending reveals, both covered by tests.

Tests used freshly transpiled changed modules with existing compiled test dependencies. The standard transpile command could not run because the existing dependency installation lacked esbuild. No full build or live WSL UI verification was performed. The broader layer check was interrupted when WSL became unresponsive and is not claimed as passing.

This is a **source-only patch** against the recorded base. It has not been backported to, applied to, or tested against installed VS Code 1.137.0 bundles. There is no combined-installer entry, and the bundle patcher is unchanged.

## Automated review follow-up

Both findings from review 5224238915 were addressed in `cc59706b4b6349329f7af86d159e654eb1864093`. Each annotation operation now owns a child cancellation source and cancels/disposes it in `finally`, releasing both the parent listener and the listener retained by `waitForState`. Asynchronous loading errors use the shared ARIA alert helper, with no announcement after confirmation disposal.

All 16 focused Chromium tests passed, including listener counts returning to zero before contribution disposal and ARIA error/disposal assertions. Targeted TypeScript diagnostics were zero; lint for all four follow-up files and Git whitespace checks passed. The refreshed GitHub patch passed reverse-apply validation. Live screen-reader and WSL UI checks have not been performed.
