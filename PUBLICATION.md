# Publication record

Published on 2026-09-09 at https://github.com/RyanEwen/vscode-patches.

The initial package contains 102 combined-patcher entries, the separate model-host-label installer, two standalone legacy patchers, 14 historical patcher snapshots, and source snapshots for all 61 RyanEwen PRs found in microsoft/vscode and microsoft/agent-host-protocol.

Public-link sections were added to and verified on 61 PR descriptions and 21 issue bodies, including the tracking epic. Existing text was retained. PR links point to their specific source/installer mapping page. Issue links point to the catalog and source index. The `public-patch-collection` markers allow later updates without duplicate comments.

Each maintained patcher entry now has a documentation page; source references and topic mappings are distinguished. See [upstream tracking](docs/UPSTREAM.md), [individual patch docs](docs/fixes/README.md), and [maintenance workflow](MAINTAINING.md).

The local directory cleanup preserved every worktree and branch. Dirty work remained local. Runtime backups, prior issue/PR bodies, worktree move records and private diagnostics are retained outside tracked repository content.
